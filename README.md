# Rusty-Json

Learning Rust by writing a simple .json parser and verify against the test cases
on [JSON_checker](https://www.json.org/JSON_checker/).

To run:
- `git clone` the repo + `cd` into the folder
- `cargo run PATH/TO/EXTRACTED_JSON_CHECKER_TEST_FOLDER`
